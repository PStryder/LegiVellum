# Deprecated Components

This `LegiVellum/components` directory is legacy and **not authoritative**.

Authoritative component implementations live in the top-level folders:
`AsyncGate/`, `CogniGate/`, `DeleGate/`, `DepotGate/`, `InterroGate/`,
`InterView/`, `MemoryGate/`, `MetaGate/`, `ReceiptGate/`.

Do not update or deploy code from this directory. It exists only for
historical reference and will be removed in a future cleanup.
